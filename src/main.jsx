import React from "react";
import ReactDOM from "react-dom/client";
import StockScout from "../StockScout.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <StockScout />
  </React.StrictMode>
);
